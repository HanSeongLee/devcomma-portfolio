import blockContent from './blockContent'
import skill from './skill';
import tag from './tag';
import project from './project';
import publication from './publication';

export const schemaTypes = [blockContent, skill, project, tag, publication];
